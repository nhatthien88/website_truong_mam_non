# cài môi trường
python -m venv .venv
.venv\Scripts\activate

# Tải thư viện
pip install -r requirements.txt

# cấu hình db: read file .env.example
import file db.sql và config file env

python run.py
- Admin: `admin` / `admin`
- Teacher: `teacher1` / `admin`



